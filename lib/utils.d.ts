import { IMessage } from './types';
export declare function isSameDay(currentMessage: IMessage, diffMessage: IMessage): boolean;
export declare function isSameUser(currentMessage: IMessage, diffMessage: IMessage): boolean;
