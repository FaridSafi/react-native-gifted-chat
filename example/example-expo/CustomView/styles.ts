import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  mapView: {
    width: 150,
    height: 100,
    borderRadius: 13,
    margin: 3,
  },

  mapViewPlaceholder: {
    padding: 5,
  },
})
