// @flow
import { Component } from 'react'
import { IMessage } from './types'

export interface SystemMessageProps<TMessage: IMessage> {
  currentMessage?: TMessage;
  containerStyle?: Object;
  wrapperStyle?: Object;
  textStyle?: Object;
}

export default class SystemMessages<TMessage: IMessage> extends Component<
  SystemMessageProps<TMessage>,
> {}
