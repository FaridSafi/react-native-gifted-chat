import React from 'react'
import Color from './Color'
import { View, Text } from 'react-native'

export default () => (
  <View style={{ padding: 20 }}>
    <Text style={{ color: Color.alizarin, fontWeight: '600' }}>
      Video is not yet implemented for the web
    </Text>
  </View>
)
